@Library("dst-shared") _

obsBuildPipeline {
    product = "shs-cn,shs-ncn"
    productStream = "slingshot-host-software"
    timeout = 30
    manifestGen = "true"
    masterBranch = "main"
    numToKeepStr = 40
}
